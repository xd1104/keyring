node_modules/
*.log
.DS_Store
Thumbs.db

# 桌面 App 啟動器用的 Python 環境（只有「開視窗」那一支需要它，server.js 仍是零依賴）。
# ⚠️ 一定要忽略：後台「存檔即發布」跑的是 git add -A ＋ push 到**公開** repo，
#    沒忽略的話這幾十 MB 會被推上去。
.venv/
__pycache__/
*.pyc

# ⚠️ 明文金鑰與每個人的加密金鑰只存這台電腦，永遠不進 repo
secrets.json
# ⚠️ 解 vault.json 的金鑰與裝置配對碼。上傳它 = 公開的 vault.json 直接被解開
vault.key
# 🧰 工具分頁的清單（從 tool-manager 搬過來）。刻意不公開，兩個理由：
#    ① 裡面是這台電腦的絕對路徑與啟動指令，只對這台機器有意義；
#    ② 它有 env 欄位，未來很可能被塞進某個工具要用的金鑰。
#    後台「存檔即發布」跑的是 git add -A ＋ push 到**公開** repo，所以一定要忽略。
tools.local.json
*.tmp~*
